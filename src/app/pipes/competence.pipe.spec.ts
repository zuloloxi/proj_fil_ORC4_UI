import { CompetencePipe } from './competence.pipe';

describe('CompetencePipe', () => {
  it('create an instance', () => {
    const pipe = new CompetencePipe();
    expect(pipe).toBeTruthy();
  });
});
